{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm cache clean --force && npm ci --include=dev && npm run build && npm run db:push"
  },
  "deploy": {
    "startCommand": "npm run start",
    "healthcheckPath": "/_health",
    "healthcheckTimeout": 300
  }
}
